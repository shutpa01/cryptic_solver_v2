# Render.com deployment config
services:
  - type: web
    name: crossword-answer-lookup
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free
